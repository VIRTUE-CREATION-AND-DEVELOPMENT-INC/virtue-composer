# @virtuecreation/composer-cli

Initialize, upgrade, inspect, and validate Virtue Composer projects.

```bash
npx @virtuecreation/composer-cli init .
npx @virtuecreation/composer-cli doctor .
```

The default installation mode uses the public `@virtuecreation/composer` npm package. Contributors working on Composer itself can opt into a local package source explicitly.
